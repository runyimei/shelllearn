#!/usr/bin/env bash

### 错误的做法
a=2
b=3
#输出的是2+3。不会进行数学运算
echo $a+$b

### 正确的写法
# 计算 10+15， 最后将25赋值给变量c
# 这种写法，在(())内，使用变量不用$。(())会自动解析变量
((c=10+15))
((c=a+b))

### 先计算10+15的值，最后用$取出计算的值，赋值给a
a=$((10+15))
b=$((a+b))

### (()) 还可以用于判断
((a>7 && b ==c))
# 输出为 1
echo $((3 < 8))

### 复杂一点的运算
echo $((100*(100+1)/2))

### 自增++ 和自减-- 运算
a=5
echo $((a++))
echo $a

